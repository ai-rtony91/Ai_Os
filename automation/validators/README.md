# AI_OS Validators

Validators are read-only checks for AI_OS work.

They report status and next safe action. They must not edit files, stage files, commit, push, approve work, release locks, resume workers, create startup tasks, create scheduled tasks, or trigger live trading.

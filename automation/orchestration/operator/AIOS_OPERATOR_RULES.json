{
  "rulebook_id": "AIOS_OPERATOR_RULES",
  "version": "1.1",
  "mode": "persistent_operator_preferences",
  "truth_source": "path_and_branch",
  "rules": [
    "Always say WHERE before commands.",
    "Always name the visible tab/window first.",
    "Always include the path before commands.",
    "Codex worker vs Git/PowerShell save tab must be distinct.",
    "CONTROL is permanent root.",
    "CONTROL is not a normal worker lane.",
    "Worker lanes are temporary/restorable.",
    "Use COPY START / COPY END markers.",
    "Never assume the user remembers lane names.",
    "Use simple wording when debugging.",
    "Prefer fixing scripts over explaining repeatedly.",
    "Do not rely on manual tab/window renaming; it does not reliably stick.",
    "Repo scripts/registry/checkpoints must own persistent titles.",
    "Path + branch are the truth source.",
    "No Codex auto-launch.",
    "Preview before launch.",
    "No commits/pushes without explicit command.",
    "No scheduled/startup tasks.",
    "No broker/API/live trading.",
    "Single-writer brainstem rule: only one Codex worker may edit overlapping orchestration/brainstem files at a time. If files under automation/orchestration/bootstrap, automation/orchestration/supervisor, automation/orchestration/operator, automation/orchestration/work_packets, docs/concepts, docs/workflows, or docs/architecture overlap, stop extra Codex workers before continuing.",
    "Any newly discovered operator workflow rule must be added to the operator rulebook or validator before continuing major work.",
    "Prefer repo-owned save/PR automation over repeated manual gh CLI commands. Manual gh commands should be fallback only.",
    "Use Invoke-AiOsGuard.ps1 before risky actions. Guard checks path, branch, lane, command type, Apply requirement, and protected-root warnings before the operator proceeds.",
    "Daily Start must connect intent, work packets, worker profiles, guard checks, and save/PR automation before asking the operator to act.",
    "Prefer Start-AiOsWork.ps1 as the primary operator entrypoint. It should consolidate status, packets, routing, validation, next action, and exact commands before asking the operator to act."
  ],
  "lane_identity": {
    "lane_id": "rulebook_codex",
    "display_title": "RULEBOOK \u00b7 codex",
    "window_title": "RULEBOOK \u00b7 codex",
    "tab_title": "RULEBOOK \u00b7 codex",
    "role": "Operator rules, workflow memory, and durable instruction capture.",
    "path": "C:\\Users\\mylab\\OneDrive\\GitHub\\aios-worker-rulebook",
    "branch": "phase-operator-rulebook",
    "truth_source": "path_and_branch",
    "manual_codex_lane": true
  },
  "blocked_actions": {
    "codex_auto_launch": "BLOCKED",
    "commits_without_explicit_command": "BLOCKED",
    "pushes_without_explicit_command": "BLOCKED",
    "scheduled_or_startup_tasks": "BLOCKED",
    "broker_api_live_trading": "BLOCKED"
  }
}

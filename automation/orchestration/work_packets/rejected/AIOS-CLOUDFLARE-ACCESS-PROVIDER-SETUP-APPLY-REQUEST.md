# AIOS Cloudflare Access Provider Setup Apply Request

Status: APPLY REQUEST ONLY / NOT EXECUTABLE / REVIEW REQUIRED

This file is not an executable Codex packet. It does not contain `AI_OS EXECUTION TOKEN` and does not authorize Cloudflare, Azure, Entra, GitHub OAuth, Google OAuth, DNS, tunnel, dashboard exposure, Turnstile, OTP, secret, credential, runtime, scheduler, queue, broker, live-trading, commit, push, merge, PR closure, or destructive cleanup actions.

## Purpose

Request a future, separately approved provider-dashboard APPLY lane for Cloudflare Access setup readiness after Anthony reviews the design, inventory, implementation plan, and provider setup review.

## Required Separate Approval

Before any Cloudflare dashboard action, a future executable packet must include:

- `CODEX-ONLY PROMPT`
- `AI_OS EXECUTION TOKEN`
- `AI_OS BOOTSTRAP REQUIRED`
- explicit Anthony approval for the exact provider dashboard action
- exact allowed provider surface
- exact forbidden actions
- no-secrets rule
- rollback path
- proof checklist
- stop point

## Non-Negotiable Boundaries

- No secrets in repo.
- Sanitized screenshots/manual confirmation only for provider UI state.
- No Cloudflare API tokens.
- No Turnstile secret key.
- No passwords.
- No recovery codes.
- No session cookies.
- No `.env` edits.
- No DNS changes.
- No tunnel creation or mutation.
- No dashboard exposure until Access policy is proven.
- No runtime, scheduler, queue, worker inbox, approval inbox, command queue, broker, or live-trading changes.

## Required Pre-Apply Inputs

- Cloudflare account/admin identity manually confirmed.
- Cloudflare second admin or independent recovery path confirmed.
- Azure Portal browser identity confirmed as AI TRADEZ dedicated browser profile.
- Recovery email class confirmed separate from primary Microsoft reset loop.
- Break-glass account owner, class, MFA, and recovery independence confirmed.
- GitHub backup login health confirmed.
- Google fallback decision recorded.
- OTP emergency mailbox class recorded or OTP deferred.
- Local Omen fallback confirmed working.

## Future Provider-Dashboard Scope Requested

The future executable packet may request approval to prepare or perform one narrowly scoped provider-dashboard step at a time. Each step must be independently approved.

Candidate future steps:

1. Confirm Cloudflare account and recovery state.
2. Draft Cloudflare Access application settings.
3. Draft deny-by-default Access policy.
4. Draft Microsoft/Entra primary provider setup.
5. Draft GitHub backup provider setup.
6. Decide Google optional fallback.
7. Decide OTP emergency fallback.
8. Plan Turnstile backend-verification setup without storing secrets.

## Stop Point

Stop after Anthony reviews this apply request. Do not execute provider setup from this file.

## Safe Next Action

Generate a complete tokenized APPLY packet only after Anthony explicitly approves the exact Cloudflare dashboard action and confirms the unresolved identity inputs.


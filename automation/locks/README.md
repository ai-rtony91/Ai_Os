# AI_OS Assignment Locks

Assignment locks record which worker owns a packet and file group during governed AI_OS work.

Locks are advisory and operator-reviewed. They do not grant automatic permission to edit, stage, commit, push, merge, or resume work.

Use locks to prevent two workers from changing the same file group without explicit approval.

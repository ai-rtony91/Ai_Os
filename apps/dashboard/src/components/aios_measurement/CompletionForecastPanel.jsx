export default function CompletionForecastPanel({ projection }) { const dimensions = projection?.dimensions || {}; const rows = [['Implementation','engineering_implementation_pct'],['Validation','validation_pct'],['Main integration','main_branch_integration_pct'],['Deployment/runtime','deployment_runtime_pct'],['Forex evidence readiness','forex_evidence_readiness_pct']]; return <section className="measurementPanel"><h3>Completion and forecast</h3><p>Inventory coverage: {dimensions.inventory_coverage_pct ?? 'Unknown'}%</p>{projection?.overall_status === 'MEASURABLE' ? <strong>Overall: {dimensions.overall_verified_completion_pct}%</strong> : <strong>OVERALL NOT YET MEASURABLE</strong>}<svg viewBox="0 0 100 65" role="img" aria-label="Verified completion dimensions">{rows.map(([label,key],index)=><g key={key}><text x="0" y={10+index*12}>{label}</text><rect x="52" y={3+index*12} width={Math.max(0,Math.min(48,(dimensions[key]||0)*.48))} height="8" /></g>)}</svg></section>; }

export default function MeasurementReceiptPanel({ projection }) { return <section className="measurementPanel"><h3>A-B-C-D receipts and blockers</h3><dl>{Object.entries(projection?.receipts||{}).map(([phase,receipt])=><div key={phase}><dt>{phase}</dt><dd>{String(receipt).slice(0,12)}</dd></div>)}</dl><ul>{(projection?.blockers||[]).map((blocker)=><li key={blocker}>{blocker}</li>)}</ul></section>; }

export default function MeasurementHealthPanel({ projection }) { const sources=projection?.source_health?.sources||[]; return <section className="measurementPanel"><h3>Source and automation health</h3><p>{sources.filter((source)=>source.parse_state==='VALID').length} of {sources.length} canonical sources valid.</p><p>Automation: {projection?.source_health?.automation||'Unavailable'}</p></section>; }

# Dashboard Data

This folder is written by `automation/orchestration/dashboard/Update-AiOsDashboardState.ps1`.
The dashboard reads `aios_live_state.json`. Do not commit live state JSON; see `.gitignore`.

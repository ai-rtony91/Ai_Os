{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "schemas/orchestration/aios_queue_admission_gate.schema.json",
  "title": "AI_OS Queue Admission Gate Preview",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "system",
    "component",
    "mode",
    "generated_at_utc",
    "input_status",
    "admission_result",
    "validated_queue_item",
    "validation",
    "safety_boundaries",
    "next_step"
  ],
  "properties": {
    "schema_version": { "const": "1.0" },
    "system": { "const": "AI_OS" },
    "component": { "const": "queue_admission_gate" },
    "mode": { "const": "APPLY_BUILD_WITH_ADMISSION_PREVIEW_OUTPUT" },
    "generated_at_utc": { "type": "string", "minLength": 1 },
    "input_status": {
      "type": "object",
      "additionalProperties": false,
      "required": ["queue_injection_preview", "proposed_queue_item"],
      "properties": {
        "queue_injection_preview": {
          "type": "string",
          "enum": ["present", "missing", "blocked"]
        },
        "proposed_queue_item": {
          "type": "string",
          "enum": ["present", "missing", "invalid"]
        }
      }
    },
    "admission_result": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "status",
        "reason",
        "queue_mutation_authorized",
        "dispatch_authorized",
        "human_approval_required"
      ],
      "properties": {
        "status": {
          "type": "string",
          "enum": ["admissible_preview_only", "blocked", "requires_approval"]
        },
        "reason": { "type": "string", "minLength": 1 },
        "queue_mutation_authorized": { "const": false },
        "dispatch_authorized": { "const": false },
        "human_approval_required": { "const": true }
      }
    },
    "validated_queue_item": {
      "type": "object"
    },
    "validation": {
      "type": "object",
      "additionalProperties": false,
      "required": ["required_fields_present", "missing_fields", "blocked", "blocked_reason"],
      "properties": {
        "required_fields_present": { "type": "boolean" },
        "missing_fields": {
          "type": "array",
          "items": { "type": "string" }
        },
        "blocked": { "type": "boolean" },
        "blocked_reason": {
          "anyOf": [
            { "type": "null" },
            { "type": "string" }
          ]
        }
      }
    },
    "safety_boundaries": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "real_queue_mutation",
        "worker_dispatch",
        "continuous_loop",
        "live_trading",
        "broker_execution",
        "credential_use",
        "unapproved_mutation"
      ],
      "properties": {
        "real_queue_mutation": { "const": "blocked" },
        "worker_dispatch": { "const": "blocked" },
        "continuous_loop": { "const": "blocked" },
        "live_trading": { "const": "blocked" },
        "broker_execution": { "const": "blocked" },
        "credential_use": { "const": "blocked" },
        "unapproved_mutation": { "const": "blocked" }
      }
    },
    "next_step": { "type": "string", "minLength": 1 }
  }
}

{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://aios.local/schemas/orchestration/aios_the_lab_execution_routing_v1.schema.json",
  "title": "AIOS The_Lab Execution Routing V1",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "contract_version",
    "request_id",
    "state",
    "authority_state",
    "action_binding",
    "issued_at",
    "expires_at",
    "provenance"
  ],
  "properties": {
    "contract_version": {
      "const": "aios.the_lab_execution_routing.v1"
    },
    "request_id": {
      "type": "string",
      "pattern": "^[A-Za-z][A-Za-z0-9_-]{7,127}$"
    },
    "state": {
      "enum": [
        "DESIGN_ONLY",
        "PREVIEW_ONLY",
        "VALIDATED_NOT_EXECUTED",
        "BLOCKED",
        "REVOKED",
        "EXPIRED"
      ]
    },
    "authority_state": {
      "const": "BLOCKED"
    },
    "action_binding": {
      "type": "string",
      "pattern": "^[A-Za-z][A-Za-z0-9_-]{7,127}$"
    },
    "issued_at": {
      "type": "string",
      "format": "date-time"
    },
    "expires_at": {
      "type": "string",
      "format": "date-time"
    },
    "provenance": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "source",
        "sanitized"
      ],
      "properties": {
        "source": {
          "enum": [
            "LOCAL_DETERMINISTIC",
            "SANITIZED_HANDOFF",
            "UNKNOWN"
          ]
        },
        "sanitized": {
          "const": true
        }
      }
    }
  },
  "x-aios-phase": 15,
  "x-aios-phase-name": "The_Lab Execution Routing",
  "x-aios-owner-authority": "AUTHORITY_BOUNDARY",
  "x-aios-owner-bundle": "OWNER-BUNDLE-3-RUNTIME-SECRETS",
  "x-aios-preparation-mode": "PREPARE_BEHIND_GATE",
  "x-aios-protected-transition": "BLOCKED",
  "x-aios-execution-capability": false
}

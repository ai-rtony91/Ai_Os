{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "AI_OS Night Supervisor Preview Route",
  "type": "object",
  "additionalProperties": false,
  "required": ["stage_id", "stage_status", "mode", "preview_only", "runtime_start_allowed", "night_supervisor_runtime_start_allowed", "telemetry_write_allowed", "control_write_allowed", "approval_inbox_write_allowed", "human_approval_required", "stop_point", "fail_closed"],
  "properties": {
    "stage_id": { "type": "string" },
    "stage_status": { "type": "string" },
    "mode": { "type": "string" },
    "preview_only": { "type": "boolean" },
    "runtime_start_allowed": { "type": "boolean" },
    "night_supervisor_runtime_start_allowed": { "type": "boolean" },
    "telemetry_write_allowed": { "type": "boolean" },
    "control_write_allowed": { "type": "boolean" },
    "approval_inbox_write_allowed": { "type": "boolean" },
    "human_approval_required": { "type": "boolean" },
    "stop_point": { "type": "string" },
    "fail_closed": { "type": "boolean" }
  }
}


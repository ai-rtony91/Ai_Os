> Historical/reference-only legacy AI_OS document.
>
> This file is not active AI_OS authority. Current operating authority is `AGENTS.md`; current front-door authority is `README.md`; current source-of-truth mapping lives in `docs/governance/source-of-truth-map.md`; current active-system mapping lives in `docs/audits/active-system-map.md`.
>
> Preserve this file for historical context and durable-rule extraction only. Do not follow stale repo paths, CLEAN-era ACTIVE_REPO references, or `docs/AI_OS` authority claims unless a future approved canonical document explicitly promotes them.

# 00_START_HERE_AI_OS_CONTEXT_PACK

## Purpose
This file is the drag/drop daily context pack starter for AI_OS work.

## Locked Architecture Snapshot
- AI_OS is **OMEN-first**.
- The OMEN desktop is the **local command/control foundation**.
- Codex + GitHub are the **implementation and repository layer**.
- AI tools operate **under** the system-level AI_OS architecture, never above it.
- Role lock:
  - ChatGPT = architect/planner/documentation controller
  - Codex = repository/code implementer
  - Claude Code = secondary reviewer/surgeon
  - GitHub = source control
  - User = execution authority
- This structure is locked unless a newer development method clearly improves the build.

## Daily Use Instructions
1. Read files `00` through `07` in order.
2. Confirm CURRENT_STATE and CHECKPOINT before making changes.
3. Execute one bounded task at a time.
4. Log unknowns as `UNKNOWN`.
5. Do not mix AI_OS system docs with trade execution changes unless explicitly required.

## Session Input Template
- Task goal:
- Boundaries (what must not change):
- Approved files to edit:
- Validation commands:
- Expected output:

## Session Output Template
- Completed:
- Not completed:
- Risks:
- Unknowns:
- Next safe step:

## Fast Start Checklist
- [ ] Architecture lock acknowledged
- [ ] Agent roles acknowledged
- [ ] Guardrails acknowledged
- [ ] Paths confirmed
- [ ] Daily report prompt prepared

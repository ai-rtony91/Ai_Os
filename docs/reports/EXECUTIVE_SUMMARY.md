# Executive Summary

Packet: AIOS-AUTONOMY-DOCS-SYNTHESIS-DRYRUN-001
Mode: DRY_RUN report creation only
Branch inspected: phase-night-supervisor-layer2-memory
Worktree: C:\Dev\Ai.Os

## Shortest Safe Path

The shortest safe path from current AI_OS state to supervised overnight development is:

```text
MCP read-only hands
-> repo knowledge graph
-> worker registry consolidation
-> append-only event log
-> canonical queue projection
-> validator routing
-> recovery and SOS policy
-> approval intelligence
-> commit package intelligence
-> autonomy scorecard
-> Night Builder read-only
```

Do not start with overnight execution. Start with safe inspection and evidence.

## Current State

AI_OS already has strong governance, schemas, DRY_RUN helpers, approval doctrine, validator doctrine, packet concepts, worker profiles, telemetry concepts, and Night Supervisor planning.

The missing layer is operational glue: one graph, one event stream, one queue projection, one worker capability authority, one approval intelligence path, and one measured autonomy scorecard.

## Top 10 Blockers

1. No active safe MCP read-only evidence bridge.
2. No runtime-loadable knowledge graph.
3. Multiple queue concepts remain active or semi-active.
4. Event schema, runtime event bus, and telemetry event shape are not reconciled.
5. Worker capability registry and window identity registry still overlap.
6. Active approval inbox file is missing from expected active path.
7. Validator routing is not closed over changed files, risk, and packet state.
8. Recovery is awareness/reporting, not qualified routine self-recovery.
9. No canonical SOS escalation policy.
10. No autonomy scorecard measuring human coordination load.

## Top 10 Existing Assets

1. `AGENTS.md` - strong Codex and AI worker authority.
2. `README.md` - clear project front door and paper-only boundary.
3. `docs/architecture/AI_OS_WHITEPAPER.md` - human-first architecture.
4. `docs/architecture/AI_OS_SEMI_AUTONOMY_MODEL.md` - autonomy ladder.
5. `docs/workflows/AI_OS_OVERNIGHT_SUPERVISOR_WORKFLOW.md` - read-only Night Supervisor doctrine.
6. `docs/governance/AIOS_NIGHT_SUPERVISOR_QUALIFICATION_GATES.md` - qualification gates.
7. `schemas/aios/orchestration/` - broad contract scaffold.
8. `automation/orchestration/validators/` - validator chain and checks.
9. `automation/orchestration/commit_packages/` - exact-file commit package preview.
10. `services/dispatcher/` and `services/supervisor/` - partial queue, scheduler, recovery, and supervisor code.

## What Not To Build Yet

- Do not install MCP yet.
- Do not point Filesystem MCP at the live repo yet.
- Do not build write-capable MCP.
- Do not launch remote Pi 5 MCP servers yet.
- Do not build autonomous APPLY.
- Do not build autonomous commit, push, PR creation, or merge.
- Do not build worker launch automation.
- Do not build scheduler persistence or daemon behavior.
- Do not mutate approval, queue, packet, lock, or runtime state automatically.
- Do not touch trading, broker, OANDA, secrets, credentials, or order paths.

## What To Build First

Build the MCP Safe Hands documentation/checklist first, then stop.

Reason:

MCP is the hands layer, but it is dangerous if installed before its read-only boundary is proven. AI_OS needs a manual, local, stdio, read-only validation path before any agent receives tool access.

## MCP Install Stop Point

Stop before:

- `npm`
- `npx`
- package install
- MCP server launch
- MCP config write
- active repo MCP connection
- remote server setup
- Pi 5 deployment

The next packet should document the safe path only. The operator installs and tests manually later.

## Unified Architecture Model

```text
Authority:
AGENTS.md, README.md, WHITEPAPER.md, governance, workflows, security, architecture

Hands:
MCP read-only local stdio access, manually proven before agent use

Memory:
runtime-loadable knowledge graph seeded from active maps

Nervous system:
append-only event log and telemetry reconciliation

Backbone:
canonical work packet queue projection and scheduler preview

Routing:
worker registry, task graph, validator router

Resilience:
conflict arbitration, recovery supervisor, self-audit, SOS policy

Human gate:
approval intelligence and commit package builder

Measurement:
autonomy scorecard and morning brief
```

## Final Recommendation

Approve one narrow APPLY packet next:

```text
AIOS-MCP-SAFE-HANDS-DOCS-APPLY-001
```

Scope it to documentation only. Do not combine it with MCP installation, queue repair, worker registry changes, runtime changes, or Night Builder execution.

One concern first: define the safe hands layer.

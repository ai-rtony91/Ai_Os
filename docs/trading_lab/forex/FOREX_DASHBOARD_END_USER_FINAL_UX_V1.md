# Forex Dashboard End User Final UX V1

Forex is not considered finished for next-project purposes until the Forex 110 gate is merged.

100% is repo-safe Forex completion. The final 10% is dashboard end-user organization and UX clarity.

The default dashboard view must show only critical status, blocker, next action, and safety state. Heavy micro-data, raw metadata, broker-heavy data, raw trading data, and noisy validator logs stay hidden behind expandable report/detail windows.

The dashboard must use emoji/picture-style clickable button and window labels. It must avoid dashboard chaos, micro-data overload, secret values, broker account identifiers, order execution data, demo authorization, and live authorization.

Bitwarden and Vaultwarden remain locked until after Forex 110 is merged. No secrets migration starts here. No token storage starts here. Broker-facing activity remains protected and separate.
